<%= form_for(@location_option) do |f| %>
  <% if @location_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_option.errors.count, "error") %> prohibited this location_option from being saved:</h2>

      <ul>
      <% @location_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
